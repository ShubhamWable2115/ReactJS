# we are using the parcel bundler same as the webpack and vite 
# parcel
-Dev build 
-Local serve
-it host your code on the server 
-It is refreshing our page means it is doing HMR
-HRM= Hot Moduke Replacement It is using the File Watching Algorithm 
-Caching -Faster build
-Parcel do the image optimization 
- Now we are using the devlopement build whenever we use the production then parcel will it will minifi and optimize as well but not fast as the Vite 